Description
Write a program to reverse a 3-digit number.

Input Format
Input consists of an integer.

Output Format
The output will be the reverse of the input integers.

Sample Input
456

Sample Output
654

#include <stdio.h>
int main()
{
   // Try out your code here
   int a, l, f, m;
   scanf("%d",&a);
   l=a%10;
   f=a/100;
   m=(a%100)/10;
   /*printf("%d%d%d", l, m, f);*/
   int res=(l*100)+(m*10)+(f*1);
   printf("%d",res);
}
